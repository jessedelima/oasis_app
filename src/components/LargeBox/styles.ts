import styled from 'styled-components/native'

export const LargeBoxWrapper = styled.View`
	background-color: ${props => props.theme.colors.secondary};
	width: 100%;
	flex-direction: row;
	justify-content: space-between;
`
